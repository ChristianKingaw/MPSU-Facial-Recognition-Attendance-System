
# Minimal client runtime requirements for the facial-recognition kiosk
customtkinter==5.2.2
deepface==0.0.95
tensorflow==2.20.0
opencv-python==4.12.0.88
numpy==2.2.6
Pillow==12.0.0
requests==2.32.5
mtcnn==1.0.0
retina-face==0.0.17

# Optional (only if using local GPU acceleration / specific models)
# tensorflow-cpu OR tensorflow (above) — install the appropriate variant for your machine
